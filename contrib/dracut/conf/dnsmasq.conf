# DNS server for dracut FQDN tests
# Resolves NFS server hostname for root=nfs:FQDN:/path tests
port=53
bind-interfaces
listen-address=192.168.50.1
no-dhcp-interface=nfs
address=/nfs-server.nfs.redhat.com/192.168.50.1
