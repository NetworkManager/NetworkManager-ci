# Check Library

all `*.sh` files from this directory are concatenated and sent to the client

## File

* does not need to start with shell header
* should contain only bash functions called within `check_client()`
* **should not** call anything outside these functions
